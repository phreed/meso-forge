// @ts-check

/**
 * @param a {number}
 * @param b {number}
 * @returns {number}
 */
function add(a, b) {
    return a + b;
  }

// for (file in output/**/*.conda) { 
//    rattler-build upload prefix --channel https://prefix.dev/meso-forge file
// }
